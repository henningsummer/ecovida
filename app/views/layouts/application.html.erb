<!DOCTYPE html>
<html>
<head>
  <title>Sistema - Opac</title>
  <link href="//netdna.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css?family=Montserrat:200,300,400,700" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css?family=Roboto" rel="stylesheet">

  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>

  <%= csrf_meta_tags %>
  <style>
    * {
      font-family: 'Roboto', sans-serif;
      font-size: 15px;
    }
  </style>
</head>
<body data-no-turbolink="true">
  <div class="modal" style="display:none;" title="">
  </div>
  <nav role="navigation" class="navbar navbar-inverse ">
      <!-- Brand and toggle get grouped for better mobile display -->
      <div class="navbar-header">
          <button type="button" data-target="#navbarCollapse" data-toggle="collapse" class="navbar-toggle">
              <span class="sr-only">Toggle navigation</span>
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
          </button>
          <%= link_to 'Ecovida', root_path, class: 'navbar-brand' %>
      </div>
      <!-- Collection of nav links, forms, and other content for toggling -->
      <div id="navbarCollapse" class="collapse navbar-collapse">
          <ul class="nav navbar-nav navbar-right">
            <%= form_tag home_search_path, class: "navbar-form navbar-left", multipart: false, method: :get do %>
              <%= search_field_tag :query, "", class: "form-control", placeholder: "Buscar em todo sistema" %>
              <button class = "btn btn-primary "><%= fa_icon 'search', text: 'Buscar' %></button>
            <% end %>

              <li><a href = "#"><%= user_name %></a></li>
              <li><%= link_to 'Sair', '/user_sessions', method: :delete %></li>
          </ul>
      </div>
  </nav>
  <div class = "container">

<% if notice %>
<div class = "alert alert-success"><%= notice %></div>
<% end %>
<% if alert %>
<div class = "alert alert-danger"><%= alert %></div>
<% end %>
<%= yield %>

<script type="text/javascript">
 const form = $('#form');

 if (form) {
   form.on('ajax:beforeSend', function(evt, xhr, settings){
     // prevent double submit
     $(':submit', this).click(function() {
       return false;
     });
   });
 }
</script>

</div>
<hr>
</body>
</html>
