<%= link_to 'Pagina ínicial', root_path %> -<%= link_to " #{@group.core.name} -", @group.core if user_type == 4 %> <%= link_to 'Listando grupos',
groups_path %> - <%= link_to "Grupo: #{@group.name}", @group %>
 - Listando gerações de certificado de <%= @group.name %>

 <% if @certificate_groups.count != 0 %>
<h1>Listando gerações de certificados para <%= @group.name %></h1>

<table class = "table">
  <thead>
    <tr>
      <th>Numero da reunião</th>
      <th>Numero da pagina</th>
      <th>Data de emissão do certificado </th>
      <th>Data de geração dos certificados</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @certificate_groups.each do |certificate_group| %>
      <tr>
        <td><%= certificate_group.meeting_number %></td>
        <td><%= certificate_group.meeting_page %></td>
        <td><%= l certificate_group.meeting_date %></td>
        <td><%= l certificate_group.created_at %></td>
        <td><%= link_to 'Mais informação', [@group, certificate_group] %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<% else %>
  <h3>Ainda não foram gerados certificados para esse grupo</h3>
<% end %>

<br>

<%= link_to 'Gerar certificado para os membros do grupo', new_group_certificate_group_path(@group), class: "btn btn-default" %>
