<%= link_to 'Pagina ínicial', root_path %> -<%= link_to " #{@group.core.name} -", @group.core if user_type == 4 %> <%= link_to 'Listando grupos', groups_path %> - <%= link_to "Grupo: #{@group.name}", @group %> - <%= link_to 'Listando unidades de produção', group_production_unities_path(@group) %> - <%= link_to @production_unity.name, [@group, @production_unity] %> - Novo escopo
<h3>Cadastrando novo escopo para <%= @production_unity.name %> </h3>

<hr>

<% if @production_unity.scope_type == 1 %>
  <%= render 'production_unity_form' %>
<% else %>
  <%= render 'agribusiness_form' %>
<% end %>
