<%= bootstrap_form_for([:records, @unity_suspension_type]) do |f| %>

  <%= show_errors(@unity_suspension_type) %>


    <%= f.text_field :name %>

  <div class="actions">
    <%= f.submit  data: {"disable-with" => "Salvando..."} %>
  </div>
<% end %>
