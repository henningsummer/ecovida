<script>
$('#<%= escape_javascript(button_name) %>').click(function(){

  $('.modal').html("<%= escape_javascript(render partial: 'documents_modal', locals: {document: document}) %>"); //inserts content into your empty div, be aware that the parameter needed to be quoted.
  $('.modal').modal(); //jquery ui will open it as a modal popup
});

</script>
